The application requires Flask to run, and is using python3 to run, so use python3-pip to install flask.

Simple exchange platform to learn how simple websites are created and use API's to function. 
